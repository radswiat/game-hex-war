const cellSize = 50
const cellWidth = cellSize * 2
const SQRT3 = Math.sqrt(3)

export default {
  TAU: Math.PI * 2,
  size: 5,
  cellSize: cellSize,
  cellWidth: cellWidth,
  cellLength: (SQRT3 * 0.5) * cellWidth,
}
